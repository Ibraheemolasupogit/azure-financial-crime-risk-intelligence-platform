name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate synthetic data
        run: python scripts/generate_synthetic_data.py

      - name: Run data validation
        run: python scripts/run_data_validation.py

      - name: Build feature tables
        run: python scripts/build_features.py

      - name: Train fraud baseline model
        run: python scripts/train_fraud_baseline.py

      - name: Run AML rule engine
        run: python scripts/run_aml_rules.py

      - name: Run ruff
        run: ruff check .

      - name: Run pytest
        run: pytest
